{
  "name": "happyfruits",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server",
    "dev": "set NODE_ENV=development& node --env-file .env --watch server",
    "test": "set NODE_ENV=production& node --env-file .env --watch server",
    "seeder": "node --env-file .env utils/seeder"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.3",
    "mongoose": "^8.2.1"
  }
}