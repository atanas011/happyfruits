## CRUD User Review

NodeJS should be installed and added to System vars.  
Thunder Client ext should be added to VS Code.

- Clone repo to local
- Add .env to backend
- cmd: cd backend && npm i && npm run dev
- Login and send reqs from Thunder Client (Reviews collection)
